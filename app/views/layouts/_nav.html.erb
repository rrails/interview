<nav class="top-bar">
  <ul class="title-area">
    <li class="name">
      <h1><a href="#"> Quizr </a></h1>
    </li>
  </ul>

  <section class="top-bar-section">
    <!-- Left Nav Section -->
    <ul class="left">
      <% if @current_user.present? %>
        <li class="divider"></li>
        <li class="active"><%= link_to('Home', @current_user) %></li>
        <li class="divider"></li>
        <li><%= link_to('Challenges', challenges_path) %></li>
        <li class="divider"></li>
        <li><%= link_to('Profile', edit_user_path(@current_user)) %></li>
        <li class="divider"></li>
        <li><%= link_to(@current_user.name, signin_path, :method => :delete) %></li>
      <% else %>
        <li>Login</li>
      <% end %>
    </ul>
  </section>
</nav>